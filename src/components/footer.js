import React from "react";

function Footer(){
    const styles = {
        color: "black",
        backgroundColor: "blue",
        fontSize: "200px"
      }

    return(
        <footer style={styles}>this is the footing!!</footer>
    )
}

export default Footer